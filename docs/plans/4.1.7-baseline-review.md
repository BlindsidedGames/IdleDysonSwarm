# 4.1.7 integration baseline — 5 September 2026

## Included sources

- Steam preparation and accepted Windows/Mac host fixes through `c2ad11f5` (includes the Cloud recovery review below).
- `codex/ids-feedback-fixes` at `5c32569a1da90296d39c4ff48414afa8a823bf30`: Terra purchase counts, allocation slider, sparse research after reset, Repeatable Research wording, Offline Time cancellation/error codes, Android/desktop Save As, all-workers defaults and localized patch notes. This parent already contains the individual feedback branches; they were not merged twice.
- `codex/4.1.7-purchase-details` at `7258bd564eda22250d8f8a760abb746ef2ced88f`: purchase wording, building scaling descriptions and patch notes.
- Matthew's existing Debug Cats destination change to `https://debugcats.blindsidedgames.com/`, copied into this isolated baseline. The original uncommitted edit in the main worktree remains intact.

## Review and integration corrections

The Electron import conflict retains one dialog import and Steam powerMonitor support. Wiki conflicts retain both branches' complete patch notes and translations; conflicting incoming translation entries were verified equal to entries in the retained superset.

Cloud recovery now offers the actual validated backup in a local/cloud conflict, preserves the damaged downloaded primary, and acknowledges its exact bytes after successful recovery. Encountering a future-version backup blocks downgrade recovery. Six tests use real files, canonical decoding and PortableSaveRepository to check both conflict choices, interrupted replacement/retry, account changes, unreadable primary publication blocking and future-version preservation.

The feedback branch changes the Quantum default to all workers. Two historical profiling fixtures implicitly relied on the previous default. Their builder now explicitly requests the original 50/50 allocation and recalculates worker counts, preserving all checked-in fixture hashes. Player reset behavior retains the new all-workers default.

## Verified

- Full combined suite: **118 files, 1,221 tests passed**.
- Lint, production build, data check, translation catalog check, generated Skill messages and authored Wiki messages pass.
- Native renderer builds; isolated Electron smoke exits 0.
- Combined Android debug APK builds with Java 21 and installs on dedicated `emulator-5554`. The native Android Save As implementation compiles.
- Extracted Android artifact contains no Steam provider SDK, App ID or achievement mappings: **706 files scanned**.
- Generated Capacitor paths caused by local dependency symlinks were restored; no machine-specific build paths are committed.

Private build/test evidence: `/Users/matthewrushworth/Builds/ids-baseline-2026-09-05/`.

## Acceptance still required

- Android Store, background/resume, Save As and restart/persistence interaction checks. Desktop CUA cannot select the emulator's unbundled Qt window; explicit approval to use ADB for UI control was requested and has not yet been received. The physical phone is untouched.
- Steam Cloud transfer/conflicts between two real machines, payment/cancellation/restoration with owned items, and Linux/SteamOS runtime acceptance remain unverified. Local recovery tests are not a substitute for provider acceptance.
- This source baseline has not been uploaded to any store. Steam Public Beta remains **25133846**; default remains **22353758**. The combined changes need a new candidate before release acceptance. No mobile release or default Steam promotion occurred.
